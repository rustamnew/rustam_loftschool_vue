.list {
    display: flex;
}

.item {
    padding: 26px;
    margin-right: 28px;

    &:last-child {
        margin-right: 0;
    }

    &.active {
        color: #383BCF;
        border-bottom: 3px solid #383BCF;
    }
}